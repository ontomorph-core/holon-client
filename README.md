# @holon/client

TypeScript client for the HOLON clinical knowledge API: concept search and lookup, drug interactions, cross-vocabulary mappings, lab reference ranges, and phenotype similarity.

```bash
npm install @holon/client
```

> Published mirror of the `packages/client` workspace in the Ontomorph HOLON monorepo. Source of truth lives upstream.
